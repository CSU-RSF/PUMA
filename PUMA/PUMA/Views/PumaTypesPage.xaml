<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PUMA;assembly=PUMA"
             x:Class="PUMA.PumaTypesPage"
             Title="Puma Types">
  <ContentPage.Content>
    <StackLayout>
      <SearchBar x:Name="search" Placeholder="Search" />
      <ListView x:Name="pumaTypesList" ItemSelected="OnItemSelected" RowHeight="70" >
        <ListView.ItemTemplate>
          <DataTemplate>
            <ImageCell ImageSource="{Binding Thumbnail}" Text="{Binding Type}" Detail="{Binding Description}" />   
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>